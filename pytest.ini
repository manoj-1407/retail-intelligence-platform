[pytest]
# Run from project root: python3 -m pytest
# This sets PYTHONPATH so both backend/ and pipeline/ imports resolve correctly
testpaths =
    backend/tests
    pipeline/tests
pythonpath = .
addopts = -q --tb=short
